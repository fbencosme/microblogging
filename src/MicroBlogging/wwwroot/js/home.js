(function() {
    "use strict";

    var home = angular.module("mb.home", ["mb.login", "mb.signup"]);

    home.controller("HomeController", [
        "$scope", "$state", function($scope, $state) {

        }
    ]);
})();